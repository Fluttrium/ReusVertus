name: Deploy to VPS

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup SSH
        env:
          VPS_HOST: 92.118.114.147
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
      
      - name: Deploy to VPS
        env:
          VPS_HOST: 92.118.114.147
          VPS_USER: ${{ secrets.VPS_USER || 'root' }}
          VPS_PORT: ${{ secrets.VPS_PORT || '22' }}
          DATABASE_URL: ${{ secrets.DATABASE_URL || 'postgresql://gen_user:std%21Y8XUk-RF%3BV@185.211.170.182:5432/default_db?schema=reusvertus' }}
        run: |
          ssh -i ~/.ssh/deploy_key -p $VPS_PORT -o StrictHostKeyChecking=no $VPS_USER@$VPS_HOST bash -s << ENDSSH
            export DATABASE_URL="$DATABASE_URL"
            mkdir -p /var/www/reusvertus
            cd /var/www/reusvertus
            
            export GIT_TERMINAL_PROMPT=0
            git config --global user.email "deploy@github-actions"
            git config --global user.name "GitHub Actions"
            
            if [ ! -d .git ]; then
              git init
              git remote add origin https://github.com/Fluttrium/ReusVertus.git
              git fetch origin
              if git ls-remote --heads origin main 2>/dev/null | grep -q main; then
                git checkout -b main origin/main
              elif git ls-remote --heads origin master 2>/dev/null | grep -q master; then
                git checkout -b master origin/master
              else
                exit 1
              fi
            else
              git remote remove origin 2>/dev/null || true
              git remote add origin https://github.com/Fluttrium/ReusVertus.git
              git fetch origin
              if git ls-remote --heads origin main 2>/dev/null | grep -q main; then
                git reset --hard origin/main
              elif git ls-remote --heads origin master 2>/dev/null | grep -q master; then
                git reset --hard origin/master
              else
                exit 1
              fi
              git clean -fd
            fi
            
            if command -v docker-compose &> /dev/null; then
              DOCKER_COMPOSE_CMD="docker-compose"
            elif docker compose version &> /dev/null; then
              DOCKER_COMPOSE_CMD="docker compose"
            else
              sudo apt-get update -qq
              sudo apt-get install -y docker-compose-plugin || sudo apt-get install -y docker-compose
              if command -v docker-compose &> /dev/null; then
                DOCKER_COMPOSE_CMD="docker-compose"
              else
                DOCKER_COMPOSE_CMD="docker compose"
              fi
            fi
            
            if [ ! -f .env ]; then
              touch .env
            fi
            
            # Устанавливаем DATABASE_URL из переменной окружения, секретов GitHub или используем значение по умолчанию
            if [ -z "\$DATABASE_URL" ]; then
              export DATABASE_URL="postgresql://gen_user:std%21Y8XUk-RF%3BV@185.211.170.182:5432/default_db?schema=reusvertus"
            fi
            
            # Удаляем старую строку DATABASE_URL если она есть
            grep -v "^DATABASE_URL=" .env > .env.tmp 2>/dev/null || true
            mv .env.tmp .env 2>/dev/null || true
            # Добавляем новую строку DATABASE_URL
            echo "DATABASE_URL=\$DATABASE_URL" >> .env
            
            $DOCKER_COMPOSE_CMD down || true
            $DOCKER_COMPOSE_CMD pull || true
            $DOCKER_COMPOSE_CMD up -d --build
            
            sleep 5
            $DOCKER_COMPOSE_CMD logs --tail=50 app || true
          ENDSSH
      
      - name: Cleanup
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
